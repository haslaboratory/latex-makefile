#!/bin/bash

PRE_COMPILE="$(pwd)/share/bin/pre_compile"
POST_COMPILE="$(pwd)/share/bin/post_compile"

export TEXINPUTS=$(share/bin/path $*)
export BSTINPUTS=$TEXINPUTS
export BIBINPUTS=$TEXINPUTS
# kpsewhich

VERSION=$(``git describe --always``)
DATE=$(``git log -n 1 --format=%ai``)

[ -f $*/config ] && . $*/config

[ -f $*/index.tex ] && file=$*/index.tex || file=$*/$*.tex

latex(){
  case $latex in
    "pdflatex" )
    pdflatex -interaction nonstopmode -halt-on-error -file-line-error -jobname=$* "\def\id{$*} \def\version{${DATE} ${VERSION}} \input{$file}"
    ;;
    "xelatex" )
    xelatex -interaction nonstopmode -halt-on-error -file-line-error  -jobname=$* "\def\id{$*} \def\version{${DATE} ${VERSION}} \input{$file}"
    ;;
    "lualatex" )
    lualatex -interaction nonstopmode -halt-on-error -file-line-error  -jobname=$* "\def\id{$*} \def\version{${DATE} ${VERSION}} \input{$file}"
    ;;
    "" )
    xelatex -interaction nonstopmode -halt-on-error -file-line-error  -jobname=$* "\def\id{$*} \def\version{${DATE} ${VERSION}} \input{$file}"
    ;;
  esac
}

bib(){
  case $bib in
    "bibtex" )
    bibtex $*
    ;;
    "biber" )
    biber $*
    ;;
    "" )
    bibtex $*
    ;;
  esac
}

mkdir -p $*/out
cd $*

$PRE_COMPILE $*

cd out

latex $*
bib $*
latex $*
latex $*

cd ..

$POST_COMPILE $*
